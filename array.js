# 使用字面量定义一个数组变量
var array = [];
var newArray = new Array();
var newArray2 = new Array(12);
# 数组的长度
var length = array.length;
# 给数组追加添加元素
array.push(1);
# 获得数组一个元素
var popItem = array.pop();
# for in遍历数组
for(index in array){console.log(array[index]);};
